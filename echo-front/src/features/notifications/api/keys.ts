/** React Query keys for the notification inbox (user-scoped, not per-workspace). */
export const notificationsKey = ["notifications"] as const;
export const notificationsSummaryKey = ["notifications", "summary"] as const;
