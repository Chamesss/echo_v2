# Hoist drizzle-orm so every workspace and every peer-dep context resolves
# to the same instance. See chat-server/.npmrc for the full reasoning.
public-hoist-pattern[]=drizzle-orm
public-hoist-pattern[]=drizzle-orm/*
